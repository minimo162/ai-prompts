# M365 Copilot通常チャット検証への方式変更

作成日: 2026-09-09
対象: `minimo162/ai-prompts`
継続する作業仕様: `CODEX_TASK_PAD_ROBIN_KNOWLEDGE.md`

## 1. 今回の優先仕様

開発用PCでは Agent Builder を利用できないため、今回の開発・受入検証は、既存のログイン済み Microsoft 365 Copilot の通常チャットで行う。公式入口は `https://microsoft365.com/chat`。Agent Builder／Copilot Studioの作成権限、ライセンス、管理者設定の調査・取得・変更は行わない。

通常チャットでの成功は、Agent Builderでの登録・検索・指示遵守・実行環境差が未検証であることと併記する。個人向けCopilot、Codex自身の生成、旧アプリの機能拡張へ無断で代替しない。

## 2. 配布物とチャット入力

- 本文へ `copilot/agent-instructions.txt` の全文、短い添付資料参照の案内、試験依頼を入力する。指示文を添付だけにしない。
- `copilot/knowledge/` の7つの `.txt` を実ファイルとして添付する。ファイル名・ローカルパスの記載だけで登録済みとは扱わない。
- `/goal`、作業依頼書、進捗ログ、評価者用期待結果はCopilotへ渡さない。T10に必要な既存フローだけを渡す。
- 8,000 UTF-16コード単位以下の共通指示正本を維持し、通常チャット用の隠れた長大指示は作らない。
- 7件を添付できない場合は、実際のエラー・上限・拒否を保存する。必要なら元ファイル名と区切りを保った結合版を機械的に作り、再生成手順と対応表を残す。アクセス制限は回避しない。

## 3. 試験手順

1. 事前確認用の通常チャットで、答えを質問文に含めず、ナレッジ固有の設定差分を質問する。回答原文を保存し、実ファイルと照合する。この会話を受入試験へ流用しない。
2. 新しい通常チャットへ最新版の指示文と同じ版のナレッジ一式を添付し、T01を本文へ入力して送信する。送信後に本文と添付の対応を確認する。
3. 回答全文・生成Robinを無修正で保存する。コードを修正してから成功扱いにしない。
4. PADの新しい専用フローへ貼り付け、回答で明示された対象設定だけを行い、設計エラー・保存・実行・成果物を期待結果と照合する。
5. 失敗は、ナレッジ不足、参照失敗、指示不足、依存説明不足、PAD操作失敗に分類する。修正後は新しいチャットへ最新版を再添付し、生成から再試験する。
6. 送信状態・回答・添付を確認できない場合は、状態を確かめず再送しない。

## 4. 継続する採取・受入範囲

元依頼書のA〜G、T01〜T10、T01・T04・T10の独立再試験、未採取構文の負例試験を維持する。既存51設定の再利用件数と、新規採取・再検証件数を分ける。PAD実測は、左パネル追加、設定確認、Robinコピー、原文保存、テキスト再読込、別空フロー貼付け、保存、実行、期待値照合、ナレッジ反映の順で行う。

各試行には、指示文ハッシュ、添付一覧とハッシュ、送信本文、回答原文、PAD条件、期待結果・実測結果、初回失敗と修正内容を記録する。合成データのみを使用し、認証情報・社内情報を保存しない。PAD未実行を合格扱いにしない。

## 5. 完了表示と未検証範囲

通常チャットで指示文とナレッジを供給し、最新版T01〜T10と追加再試験がPADの成果物照合まで合格した場合の表示は次のとおりとする。

> 通常のM365 Copilotチャット＋PADで検証済み。Agent Builderの登録・検索・指示遵守・実行環境差は未検証。

Agent Builder未検証だけを理由に全体をBLOCKEDへ戻さない。それ以外の必須項目が残る場合は、項目ごとに未完了・未検証として報告する。push、PR、マージ、公開、旧アプリの機能拡張は行わない。
