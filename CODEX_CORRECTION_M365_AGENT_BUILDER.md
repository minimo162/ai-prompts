# M365 Copilot内Agent Builderへの修正方針

作成日: 2026-09-09
対象: minimo162/ai-prompts
継続する作業仕様: `CODEX_TASK_PAD_ROBIN_KNOWLEDGE.md`

このファイルは、元依頼書と食い違う利用先・再開条件を訂正する。既存成果物、採取原文、証拠は捨てずに再利用する。

## 1. 利用先

対象はMicrosoft 365 Copilotの画面内にある「エージェント → 新しいエージェント／エージェントの作成」から開くAgent Builderである。公式入口は `https://microsoft365.com/chat`。Copilot Studio単体の開発ポータルを利用先にしない。

作成物は短い指示文とRobin技術ナレッジを参照してコードを回答する本人限定・検証専用エージェントである。PADへの貼付け・実行は利用者が行う。トピック、クラウドフロー、Dataverse環境、コネクター、PAD自動実行基盤、Studio単体への移行、試用開始、ライセンス購入、管理者設定変更は新設・実施しない。

Agent Builder内部にCopilot Studioの表示があっても、入口、画面構成、接続先を確認せず別製品と断定しない。仕様確認はM365 Copilot／Agent Builder公式資料を優先し、Studio単体の説明を流用しない。

## 2. 前回の停止理由の訂正

Copilot Studio単体で表示された作成権限・管理者ライセンスエラーは、失敗履歴として保存する。ただし、それをM365 Copilot内Agent Builderの再開条件とはしない。まずログイン済みM365 Copilotから正規入口を開き、意図した組織・アカウントであることを確認する。無断で別テナントへ切り替えない。

Agent BuilderのConfigure／構成画面で、名前・説明・指示・ナレッジが確認できるかを実測する。正しい入口でも拒否された場合だけ、到達手順、日時、エラー全文、未確認事項を記録する。「必要なライセンス」を推測で指定しない。

## 3. 正しいエージェントでの試験

1. `copilot/agent-instructions.txt` を指示欄へ入力する。
2. `copilot/knowledge/` の7つの `.txt` をナレッジとして登録する。普通のチャット添付だけでは登録済みとしない。
3. 処理完了後、指示文・ファイル一覧・版を見直す。
4. ファイル内の設定差分を質問文に答えとして含めず尋ね、一般論ではなく登録ナレッジを参照できるか確認する。
5. T01からT10と追加再試験を行う。プレビューと保存後のエージェントを区別する。

登録後にローカルファイルを変更した場合は、その版を再登録してから再試験する。参照ファイルのハッシュと結果を対応づける。PADが使えない場合、生成回答の採取までは進めてよいが、実行結果はNOT_RUNのままにする。

## 4. PAD接続の切り分け

Computer UseにPADが見えないことだけで、PADが使えないとは結論づけない。対象Windows・対話セッション、PADコンソール／Designerの起動状態、最小化・別ウィンドウ・別デスクトップを確認し、許可された既存のUI Automation補助を読み取り・専用フローへ限定して再利用する。無断の管理者実行、プロセス強制終了、既存業務フローの停止・消去、操作基盤の新規開発は行わない。

PAD採取とAgent Builder試験は別管理する。画面とクリップボードの操作は直列に行う。

## 5. 必須範囲の採取

既存51設定の再利用件数と今回新規に採取・再検証した件数を分ける。coverageへ不足を書くだけでは対応済みにしない。A〜Gの不足から最小の1件を選び、左パネルから追加、設定、Robinコピー、原文保存、保存ファイルからテキストだけを再読込、別の空フローへ貼付け、保存・実行、期待値照合、ナレッジ反映までを完了する。

原文、前提、PAD版・言語・Power Fx、各段階の結果を残す。非テキストクリップボード情報や前回成果物に依存しない。推測したRobinを採取済みとしない。

## 6. 配布物の点検

指示欄は8,000文字以下。ナレッジは実測構文・設定差分・前提・例の技術資料であり、命令書の長文化で制限を回避しない。7つの `.txt` にRobin本文を収録し、ローカルパスやGitHubリンクを開かないと意味が分からない構成にしない。採取原文、加工テンプレート、実行済み、未実行を区別する。未確認の構文・UI要素を作らず、不足を具体的に伝える。

## 7. テストの分離

`Test-Copilot.ps1` は停止箇所・前提・今回変更との関係を読んで扱う。今回の回帰だけを直し、旧アプリ開発へ戻らない。既存全体テストと今回の指示文・ナレッジ整合性検査を分け、未完走をPASSにしない。

T01〜T10は生成回答を無修正でPADへ貼り、明示された設定だけを行い、成果物を照合する。失敗した回答を上書きしない。通常CopilotやCodexの成功を今回のエージェントの成功に置き換えない。

## 8. 反復と完了

この修正方針を参照し、まず誤った再開条件を訂正してM365 Copilot入口とPAD接続を確認する。その後、可能な方で1件を検証し、実ファイルと証拠を残して次へ進む。元依頼書第10節の完了条件を維持する。

BLOCKEDは該当項目だけに付け、他の実行可能な作業を続ける。同じ失敗を同じ方法で2回繰り返したら観測を更新する。全残作業が外部依存または利用・承認制限で止まった場合のみ、未完了として具体的な再開条件を保存する。

旧アプリ改修、push、PR、マージ、公開は行わない。ただし本人限定の検証用Agent Builderエージェントの作成・保存・プレビュー試用は対象に含める。
