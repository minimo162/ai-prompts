1. 前提・対象範囲・未確認点

対象:

入力ブック: C:\Users\yuuki\ai-prompts\catalog\fixtures\excel\excel-catalog.xlsx
読み取り範囲: A1:B2 のみ
出力ブック: C:\Users\yuuki\ai-prompts\catalog\evidence\normal-chat-t04-output.xlsx
元ブックは変更しない
条件一致行の抽出、または集計結果の保存を希望

確認済みナレッジでは、Excelについては以下が実測済みです。

Excel起動
Excelワークシートからの A1:B2 範囲読み取り
Excelワークシートへの書き込み
別名保存
保存
終了

一方で、今回の要件に必須となる次の構成要素は、索引上で未採取または未確認です。

データテーブルの行反復
行抽出
Excel読み取り結果のセル参照
シート選択
Excel範囲読取結果を条件判定へ渡す組合せ
集計処理の実行例

索引では「行反復・セル参照は未採取」「シート選択とデータ反復は未観測」と明記されています。

したがって、

「どの列を条件判定するか」
「条件値は何か」
「集計方法（件数・合計・最大値など）は何か」

を指定いただいても、現状ナレッジだけでは実測済みRobinとして安全に組み立てられません。未確認のデータ反復や行抽出構文を推測生成すると、依頼条件に反します。

2. PADで先に確認・採取が必要な項目

T04を実測根拠付きで完成させるには、まず次のいずれか1件の採取が必要です。

「Excel ワークシートから読み取る」で A1:B2 をデータテーブルへ読み取った後、1行ずつ処理する実測例
データテーブルの列参照実測例
データテーブルから条件一致行を取得する実測例
Excelへ結果を書き戻す際のシート指定実測例

採取後であれば、条件一致行の保存または集計結果保存のRobinを根拠付きで作成できます。

3. 貼り付け用Robin

現在のナレッジでは、条件一致行抽出または集計保存に必要な「データ反復・行抽出・セル参照」の実測原文が不足しています。

そのため、依頼条件を満たす貼り付け用Robinは生成できません。

4. デザイナー上で確認する事項

確認済み範囲としては、Excelの以下の操作のみが索引に記録されています。

Excel起動
A1:B2 範囲読み取り
ワークシート書き込み
保存
閉じる

ただし、今回必要な

条件判定対象列の参照
行単位抽出
件数集計
合計集計
フィルター済み結果生成

の確認済みRobinは見当たりません。

5. 保存・実行・照合手順

未確認の行抽出・集計構文が含まれるため、この時点では保存・実行・期待結果照合まで完了する手順は提示できません。

再開条件:

Excel A1:B2 読み取り後の処理部分について、PADからコピーしたRobin原文
またはデータテーブル反復／行抽出アクションの採取結果

のいずれかが取得できれば、その実測原文を根拠としてT04のフロー案を作成できます。