1. 前提・対象範囲・未確認点

対象は、合成Excel C:\Users\yuuki\ai-prompts\catalog\fixtures\excel\excel-catalog.xlsx の範囲 A1:B2 を読み、条件一致行または集計結果を別ブック C:\Users\yuuki\ai-prompts\catalog\evidence\normal-chat-t04-output.xlsx へ保存するフローです。

しかし、提供されたナレッジでは以下が未採取または未確認です。

Excel範囲 A1:B2 の読取りアクション自体は観測・実行確認済み。元ブック不変も確認済み。
データ反復（行の列挙）、行抽出、行ごとの条件判定をExcel読取り結果へ適用する組合せは未確認。
Excelシート選択の独立例は未確認。
Excelデータから集計結果を作成する実測例は未確認。
別Excelブックへ条件一致行または集計結果を書き込む完成組合せは未確認。

そのため、

「条件一致行を抽出」
「件数集計」
「合計・平均などの集計」
「抽出結果を新規xlsxへ保存」

を含む完成Robinを、確認済み原文として出力することはできません。未確認のデータ反復・行抽出・シート操作を推測して生成することは、依頼条件とナレッジ利用規約に反します。

2. PADで先に確認・追加採取が必要な項目

次の最小単位をPADで1アクションずつ採取してください。

「Excel ワークシートから読み取る」

範囲 A1:B2
出力変数型

読み取った結果に対する

行反復（For each 等）
行データの参照構文

条件判定

特定列との比較方法

集計

件数カウントまたは数値集計

出力用ブック

新規Excel作成
ワークシート書込み
xlsx保存

各アクションについて、

PADへ追加
保存
Robinコピー
別空フローへ再貼付け
実行
結果照合

まで完了した原文が必要です。

3. 貼り付け用Robin

現時点では出力しません。

理由は、依頼で必須となる

条件一致行の抽出
集計処理
別ブック保存

に必要な実測済みRobin原文がナレッジ内に存在せず、完成コードを作ると未確認構文の推測になるためです。

4. デザイナー上で確認する項目

確認済み範囲としては、

Excel起動
Excel範囲読取り（A1:B2例を含む）
Excel書込み
Excel保存
Excel終了

が観測済みです。

ただし、

読取り結果の型
行数取得
行反復
行抽出
集計結果生成

は確認済み範囲に含まれていません。

5. 合成データでの検証手順

追加採取後に実施します。

入力ブックのハッシュまたは内容を記録。
フロー実行。
出力ブック normal-chat-t04-output.xlsx を確認。
条件一致行または集計結果が期待値と一致することを確認。
元ブック excel-catalog.xlsx が未変更であることを確認。
同一条件で再実行し、出力が再現することを確認。

結論: 提供ナレッジの確認済み範囲だけでは、T04の「条件抽出または集計結果を別Excelへ保存する完成Robin」は作成できません。行反復・行抽出・集計・別ブック保存の実測原文を追加採取いただければ、その原文だけを根拠にフロー案を作成できます。