PAD-Robin-Knowledge-Bundleを確認しました。T08の目的は明確ですが、指定された欠損パスを使い、エラーを記録して続行を停止する貼り付け用Robin全体は、結合版に完全な実測原文が収録されていないため出力しません。

1. 前提・対象範囲・未確認点

目的: 存在しない合成入力ファイルを意図的に読み取り、ファイル欠損時のエラー状態と記録だけを確認する。

入力: C:\Users\yuuki\ai-prompts\catalog\fixtures\text\t08-missing-20260910.txt

想定出力: 正常時のテキスト変数。ただし今回は欠損ファイルなので生成を成功条件にしない。

保存先: なし。

上書き: なし。

後続処理: 書込み、完了マーカー、公開処理を配置しない。

成功条件: ファイル読取りが欠損エラーになり、エラー情報を確認でき、後続の副作用が一切発生しないこと。

ナレッジには、UTF-8の「ファイルからテキストを読み取る」のコピー原文があります。また、欠損ファイルによるランタイムエラーと、既定の「すべてのエラー」を処理するハンドラーが2回完了した記録があります。

一方、結合版には次の完全なRobin原文が掲載されていません。

BLOCK

ON BLOCK ERROR

既定の「すべてのエラー」の設定部分

エラー情報を取得または記録するアクション

ブロック全体の正確な終了構造

根拠として示されているのは catalog/evidence/p3-error-default-handler-flow-20260911.robin という原文パスと検証結果だけで、そのファイル本文は結合版に含まれていません。名前付き FileNotFound の処理はランタイムエラーが残ったため、成功例として使用できません。

2. PADで作成する最小構成

専用の空フローを作成し、まず次の1アクションだけを手動で設定してください。

「ファイルからテキストを読み取る」を追加する。

ファイルパスに指定の欠損パスを設定する。

エンコードを、確認済み例と同じ UTF-8 にする。

出力変数名は FileContents とする。

その後ろにはアクションを置かない。

書込み、完了マーカー、公開、送信、削除のアクションは追加しない。

この単独構成なら、欠損時点でフローがエラー停止するため、後続処理が実行される余地を作りません。ただし、これは「エラーを捕捉して変数へ記録する構成」ではなく、PADの実行エラー表示を確認するための安全な最小負例です。

3. 貼り付け用Robin

今回はコードブロックを出しません。

指定パスを含む読取り行自体は、既存の確認済みアクションを基に置換できます。しかし、依頼の中心である「エラー状態を記録し、捕捉後も後続処理を実行させない」部分について、完全な実測Robin原文が結合版にありません。未採取のエラー処理構文を補完すると、確認済み原文だけに限定する条件に反します。

4. デザイナーと実行時の確認

単独の読取りアクションを保存した後、次を確認します。

デザイナー上でアクション数が 1 である。

対象パスが指定された欠損ファイルと完全に一致する。

エンコードがUTF-8である。

読取りアクションの後ろにアクションが存在しない。

実行時に読取りアクションで停止する。

PADが表示したエラー全文を保存する。

エラーが発生したアクション名、PAD版、表示言語、Power Fx状態も記録する。

FileContents に正常な読取り結果が生成されたとは扱わない。

エラーが表示されない場合も成功扱いにせず、対象ファイルが誤って存在していないか確認する。

実測環境として結合版に記録されている条件は、PAD 2.71.115.26224、日本語UI、Power Fx OFFです。

5. 合成データでの検証手順

指定パスにファイルが存在しないことを、PAD実行前に確認する。

専用フローを複製する。

複製側に読取りアクション1件だけを設定する。

保存後、アクションをコピーして原文を採取する。

別の空フローへ、その原文をファイルから再貼り付けする。

デザイナーエラーがないことと、アクション数が1件であることを確認する。

1回実行し、欠損ファイルのエラー全文を採取する。

出力ファイル、完了マーカー、公開結果が作成されていないことを確認する。

原因確認前に繰り返し実行しない。

エラー捕捉と記録をRobinへ含めるには、PADで「ブロック エラー発生時」を既定の「すべてのエラー」、続行ON、再スローOFFに設定し、使用する記録アクションも含めてコピーした完全な原文を追加採取する必要があります。その原文が得られるまでは、単独読取りによるエラー停止をT08の安全な確認範囲とします。
