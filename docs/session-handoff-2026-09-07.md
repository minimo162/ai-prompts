# 次セッションへの引き継ぎ（2026-09-07）

## 最初に扱うこと

ユーザーが求めているのは、CodexやClaude Codeのように目的に応じて作業を組み立てる汎用AIエージェントです。CSVなど特定フォーマット専用の製品を目指していません。現アプリの拡張を既定路線にせず、まず方式を再検討してください。

ユーザー申告による社内条件は次のとおりです。組織側への確認はまだ行っていません。

- 自部署で現時点で認められているAI製品はM365 Copilotのみ。
- Copilot Coworkは社内では利用できない。
- 他部署にはCopilot Studio、GitHub Copilot、Azure OpenAI APIの利用例があるが、自部署で使えるかは未知数。
- 社内導入のしやすさを重視するが、明確な判断基準は未確定。
- M365 Copilotの利用許可だけで、CDP自動操作・API・自作アプリ・PAD連携まで許可されるとは判断しない。

比較対象は、現アプリ＋PAD＋Copilot、PADフロー生成を助けるプロンプト中心の運用、minimo162/Misenのような自作エージェント、既存のGitHub Copilot系エージェント製品、Copilot Studio／Azure OpenAIを含む別方式です。製品の最新仕様は公式情報で確認し、機能として可能なことと社内で利用できることを分けてください。Misenの構成も現物を読むまで推測しないでください。

## 保存済みの実装

リポジトリは `C:\Users\yuuki\ai-prompts`、originは `https://github.com/minimo162/ai-prompts.git`。

- PR #15でCSV処理・復旧・配布・診断などの途中成果を保存。
- PR #16をsquashマージし、製品のチェックポイントは `c1eb3108e99a580a1c2b037fe7aca6c986874af3`。元作業先端 `c89710a` とtree一致を確認。作業ブランチはローカル・リモートとも削除済み。
- 今回の引き継ぎ更新は文書とIssueのみ。セッション開始時に最新mainとPR状態を再確認すること。
- 配布対象はCMD・App.ps1・index.htmlの3ファイル。GitHubへのマージは正式Release・社内共有への展開・受入完了を意味しない。

現在の実装は、汎用依頼を主画面に戻し、CSV分類を補助機能として折り畳み表示しています。PAD実採取カタログは11種類・17設定例。文字列、リスト、数値変換、有限ループを検証器へ接続しました。入力と成果物の証拠を分けた完了判断、ジョブ固有IDによる成果物オープン要求、実行時本文プレビューがあります。任意操作に対応する汎用エージェントの完成ではありません。

## 証拠と限界

- [非ライブ検証](nonlive-verification-2026-09-07.md)：全36スイート。初回34 PASS／2 FAIL、試験基盤を修正して該当2試験を再実行しPASS。初回一括PASSではありません。製品3ファイルは不変。
- [汎用依頼の実機記録](general-agent-live-2026-09-07.md)：実M365＋PADで置換、分割結合、数値ループ、数値変換の個別ケースが成果物作成・完了判断まで到達。各候補の結果を区別しています。
- 最新製品App SHA256は `1f376113a498eef977f611081cd94403ae16d78a926cef1ec38cc69049333ccf`。同候補の数値変換ケースは `87.6 → 88.60`、83,268ms、PAD1回・完了判断1回でDONE。
- [CSV実装と検証の履歴](issues-8-14-progress.md)、[会話分離と測定](live-conversation-isolation-2026-09-07.md)：旧固定候補の1／50／100件・長文成功は、最新候補の受入や意味品質の証明にはしません。
- [成果物表示](general-artifacts.md)：Windowsへのオープン要求は確認済み。関連付け選択画面が出たため、外部アプリでの本文表示完了とは扱いません。
- GitHub ActionsはPR #16マージ時点で未設定。ローカル試験をCI成功と呼ばないこと。
- 詳細な `.work` 証拠は同一開発PCのGit管理外。文書の参照だけで別環境から独立検証済みとはしないこと。

## Issueの扱い

総合Issue #5と関連Issue #8〜#14はOPENを維持します。今回の冒頭更新を現在地として読み、元本文のCSV最優先・自由生成Robinを互換経路に限定する提案を、ユーザーが確定した方向性として扱わないでください。既存の受入条件は自動達成・削除せず、方式決定後に再整理します。

|Issue|今回の到達点と残課題|
|---|---|
|#8|CSV分類の実装・旧候補の測定は保存済み。補助機能として扱い、CSV完成を汎用化の前提にしない。意味品質、人時間、別PC受入は未完了。|
|#9|実採取Robinの型検査・有限ループ・入力証拠・完了判断を追加。For each、複数入力の証拠、Excel/PDF/ブラウザー、長い成果物の一般的完了検証は未完了。|
|#10|PAD保全・明示復旧、CSVの既存回答照合・未送信分継承を実装。native Save失敗・実機復元・認証/中断復旧全体の受入は未完了。|
|#11|汎用依頼を主画面化し、成果物ID・実行時プレビューを追加。初回案内、汎用の追加依頼と再計画、初見利用者・倍率等の受入は未完了。|
|#12|要求単位の会話分離・完了判断と実機の個別成功を記録。接続方式・承認条件の比較を先に行う。一般的な安定性保証は未完了。|
|#13|3ファイル封入・旧版復帰等の実装とローカル検証を保存。社内SMB・権限・別利用者・保存規則の受入は未完了。|
|#14|非ライブ36スイートと候補別実機記録を整理。意味品質、2台2利用者、社内受入は未完了。|

## 次セッションへの依頼文

> `C:\Users\yuuki\ai-prompts` を引き継いでください。まず最新main、Issue #5・#8〜#14の本文と全コメント、この文書、関連するObsidian記録を確認してください。
>
> 今回は機能追加から始めず、根本的な方式を検討してください。目的はCodexやClaude Codeのような汎用AIエージェントであり、特定のCSVや帳票形式への特化ではありません。自部署で認められているのはM365 Copilotのみで、Copilot Coworkは利用できません。他部署のCopilot Studio・GitHub Copilot・Azure OpenAI APIの利用例はありますが、自部署の利用可否は未知数です。社内導入のしやすさを重視します。
>
> 現アプリ＋PAD＋Copilotの継続、PADフロー生成プロンプト中心への回帰、minimo162/Misenのような自作、既存エージェント製品の利用、その他の方式を比較してください。実現できる汎用性、導入に必要な権限・申請、保守負担、操作の安定性、費用、既存資産の再利用を整理し、今使える条件でできることと追加承認が必要なことを分けて、推奨案と小さな検証案を示してください。未確認の社内基準を推測して断定しないでください。
>
> 現実装はPR #16で保存済みのPoCです。既存資産を捨てることも継続することも前提にせず、方式を決めるまでは新しい機能追加や移行を始めないでください。過去の失敗・unknown・claim・検証成果物は保持し、異なる版の成功を現在の受入へ流用しないでください。

## 実装を再開する場合の注意

Windows PowerShell 5.1を使用し、日本語を含むPS1のUTF-8 BOMを維持してください。製品3ファイルを編集した場合は再封入して検証します。Robinの生コピーと封入済みファイルは改行・特殊文字を含むバイトを保持します。稼働中のPAD・Edge・サーバー、所有記録・プロファイル・対象フローは再確認し、過去のPIDや消費済みhelper/claimを再利用しないでください。
