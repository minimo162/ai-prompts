# Issues #8〜#14 実装・受入の現在地

状態: **実装途中。7件とも未完了・出荷未承認。**

2026-09-07、`C:\Users\yuuki\ai-prompts`、`minimo162/ai-prompts`、開始時main `a51615a`。Issue #8〜#14の本文と全コメントを取得（各コメント0件）。原本Issueの受入条件は緩和しない。旧Issue #5の記録は保持する。

## 実装した経路

- PS1内の固定CSVパーサー。UTF-8/BOM、明示CP932、引用符・改行・カンマ・任意列・原IDの文字列保持。重複IDや空ID/本文は結合せず各行へ対象外理由を残す。
- 明示選択した1〜10ファイル・合計1〜100行・1MiBのマニフェスト。原本ハッシュ固定、実行前の変更検出、同名別フォルダーの識別。この上限は実M365のベンチ値ではない。
- 分類候補・条件・本文を1バッチ最大5行・要求24,000文字で処理。要求ID／行ID／候補／理由／状態を照合。任意列・入力パスは意味処理へ送信しない。
- 対象確認→サーバー上の計画ID・ハッシュへの承認→PS5子プロセス→分類→全量検証→新規成果物。CSV処理にPADは不要。従来のRobin/AiCall経路は維持。
- 原値正本JSON、分類CSV、要確認CSV、未処理CSVを新しい出力領域へ保存。Excel閲覧用CSVは数式風セルにアポストロフィを追加。Excelの自動型変換は保証しない。原値・任意列はJSONに保持。
- 試行ごとの送信前／プロバイダー進入／受信／確定記録と結果ハッシュ。Copilot送信予約後のタイムアウトは不明。未送信分の続行と確定結果の再読取り。claimを削除しない。
- HTMLの原文・結果比較、絞り込み、停止／続行、成果物IDからの検証付きオープン。ファイル選択は別プロセスのダイアログ（ネイティブ操作は未検証）。
- `-OfflineTest`を子プロセスへ伝播。Copilot呼出し・HTTP・認証画面起動・PAD実行を拒否。既存UI試験は空ジョブ領域・プロファイル不存在・offlineモードを開始前に検証。
- 問い合わせ情報を許可項目だけから生成し、画面プレビューとローカル保存。業務本文・生エラー・認証情報・個人パスを含めず、自動アップロードしない。

## 検証入口

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -STA -File tests\Run-NonLiveTests.ps1 -Suite Csv
# 開発端末にPlaywright/Edgeがある場合だけ追加。利用者配布の依存には含めない。
powershell.exe -NoProfile -ExecutionPolicy Bypass -STA -File tests\Run-NonLiveTests.ps1 -Suite Csv -IncludeUi
```

`-Suite All`は既存回帰も実行する。`.work/nonlive-*/verification.json`は実行した3ファイルのハッシュを記録する。実M365・native PAD・意味品質・別PCはNOT_RUNのまま残し、非ライブPASSを出荷承認にしない。

追加契約試験はCSV73、バッチ17、ライフサイクル19、隔離・診断9項目。画面試験は実Edge／HTTP／PS5子プロセスと**置換した分類プロバイダー**を使う。50行の対象確認、画面停止、20行保持・30行続行、送信IDの重複0、要確認3行、原本不変、再読込み、狭幅、診断プレビュー・保存を検証する。実M365の意味精度やnative PAD受入には数えない。

画面検証で状態ファイルの読取りとReplaceの共有違反・短い不存在を観測。削除共有付き読取りハンドルと、限定した読取り再試行を追加。業務処理は再実行しない。

## 残る必須作業

|Issue|次に実装・検証するもの|
|---|---|
|#8|要確認への追加指示、50行正解データと意味採点、直接Copilot／固定処理との人時間比較、実M365の1/50/100行、別PC・別利用者|
|#9|現状は固定CSVプリセット。Copilotが型付きアクションを選び、観測から再計画する実行器、未知操作/許可外引数の全体事前拒否、共有入力のステージング、UNC/ロックの実機検証|
|#10|実worker強制停止・書込み前後の障害注入、送信確認契約、PAD Main/ownerの永続バックアップ・条件付き復元、clipboard復元警告、未知状態の照合案内|
|#11|ネイティブ選択の実機確認、過去ジョブ選択、認証復帰の接続確認、要確認への追加指示、PAD初回案内、成果物差替え/削除拒否試験、5人試行・倍率・複数モニター|
|#12|対応条件/adapter版集約、送信予約と送信確認の永続区別、匿名fixture、実長文/連続要求ベンチ、ポリシー無効時の案内、公式方式の再確認と採否記録|
|#13|3ファイル対応ハッシュ宣言、公開中断/混在拒否、公開/ACL手順、明示rollbackとschema互換性、保持方針、現在版/成果物版表示、実共有受入|
|#14|全受入条件の機械可読索引、同じ凍結3ファイルでのゲート、過去版E2E流用拒否、PAD/Edge・保全状態の診断拡張、全回帰、実M365/PAD/E2E・社内PCを含む2台2利用者|

PR・マージ・正式リリース公開・社内共有変更は未実施。この文書や非ライブ試験だけでIssueを閉じない。
